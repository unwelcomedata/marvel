# marvel — dependencies actually used by this project.
# (Trimmed from the template toolkit: no playwright — the sources are static
#  HTML; no geopandas — no maps; no dotenv — no API keys.)

# --- core data ---
pandas==3.0.5
duckdb==1.5.5
pyarrow==25.0.0

# --- web ingestion (static HTML tables) ---
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.4.0

# --- notebooks ---
jupyterlab==4.4.3
ipykernel==6.29.5

# --- viz (matplotlib exploration + Pillow social/web export) ---
matplotlib==3.10.3
Pillow==12.3.0

# --- data packaging ---
openpyxl==3.1.5             # Excel export

# --- config ---
pyyaml==6.0.2
